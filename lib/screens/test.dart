import 'package:flutter/material.dart';

class TestResultsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Test Results'),
      ),
      body: Center(
        child: Text('Test Results Screen'),
      ),
    );
  }
}
